# 生产/测试：OceanBase（MySQL 租户模式）/ MySQL 8，连接信息通过环境变量注入
spring:
  datasource:
    url: ${MIRAGE_DB_URL:jdbc:mysql://localhost:3306/mirage?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    username: ${MIRAGE_DB_USER:root}
    password: ${MIRAGE_DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
